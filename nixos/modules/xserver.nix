{
    services.xserver = {
    # Enable the GNOME Desktop Environment.
    enable = true;

    # Enable the GNOME Desktop Environment.
    displayManager.gdm.enable = true;
    desktopManager.gnome.enable = true;    

    # Configure keyboard
    xkb = {
        variant = "";
        layout = "fr";
    };
  };
}